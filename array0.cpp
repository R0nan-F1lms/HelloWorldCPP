#include "splashkit.h"
#include <stdio.h>

int main() {
    // Define an array with some values
    int array[] = {1, 2, 3, 4, 5};

    // Access each value directly and print it using printf
    printf("First value: %d\n", array[0]);
    printf("Second value: %d\n", array[1]);
    printf("Third value: %d\n", array[2]);
    printf("Fourth value: %d\n", array[3]);
    printf("Fifth value: %d\n", array[4]);

    return 0;
}